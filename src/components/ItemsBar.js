import Button from './Button';

const ItemsBar = () => {
  return (
    <div className="items-bar">
      <Button Name="All" />
      <Button Name="Auction" />
      <Button Name="Buy it now" />
    </div>
  );
};
export default ItemsBar;
